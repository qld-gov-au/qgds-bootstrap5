  <div class="form-check">
    <input class="form-check-input" type="{{type}}" name="{{name}}" id="{{id}}" value="{{value}}" {{state}}>
    <label class="form-check-label" for="{{id}}">
      {{label}}
    </label>
  </div>
