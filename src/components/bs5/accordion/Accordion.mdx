import { Canvas, Meta, Story, Source } from "@storybook/blocks";
import * as AccordionStories from "./accordion.stories";
import defaultdata from "./accordion.data.json";
import "../../../stories/storybook-documentation.scss";
import { SharedNote } from "../../../stories/sharedNote.mdx";
import { designResources, usageInstructions } from "./docs.data.js";

<Meta of={AccordionStories} />

# Accordion

<Canvas>
  <Story id="accordion-default" of={AccordionStories.Default} />
</Canvas>

<h2>Documentation</h2>
<p>Click on the buttons inside the tabbed menu:</p>

<SharedNote
  defaultdata={defaultdata}
  componentId="accordion-default"
  designResources={designResources}
>
  <div dangerouslySetInnerHTML={{ __html: usageInstructions }}></div>
</SharedNote>
