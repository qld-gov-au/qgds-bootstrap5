This is the scss folder, that is converted to css for dist. 

To match pathing the folder is 'css' even though it should be 'scss'.

`/flags/qld-variables-dark.scss` is the additional variables file with the overriding flag palette or css variables. This would impace the main theme bundled css which has separate export
